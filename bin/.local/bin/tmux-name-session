#!/usr/bin/env zsh

dir="$PWD"

git_root=$(git -C "$dir" rev-parse --show-toplevel 2>/dev/null)

if [[ -n "$git_root" ]]; then
  name="${git_root:t}"
  dir="$git_root"
else
  name="${dir:t}"
fi

[[ -z "$name" ]] && name="home"

exec tmux new-session -A -s "$name" -c "$dir"
